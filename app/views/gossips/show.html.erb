<h1><%= @gossip.title %></h1>

<p><%= @gossip.content%></p>

<p>Auteur : <a href="<%= user_path(@user.id)%>"><%= @gossip.user.last_name%> <%= @gossip.user.first_name%></a>
<p>

    <a href="<%= gossips_path %>" class="btn btn-primary">Retourner aux gossips</a>

    <a href="<%= edit_gossip_path(@gossip.id)%>" class="btn btn-secondary">Editer</a>

    <%= link_to "Supprimer", gossip_path(@gossip.id), class:"btn btn-outline-danger", data:{confirm:"Etes vous sûr de vouloir supprimer ce potin ?", method:"DELETE"} %>

</p>